s="ramteja"
print(s)
print(len(s))
''' strings are also starts with 0 and ends with n-1.
    we have the negative indexing also there which starts from
    last position in the string.
    types are length, slicing, interval
'''
#slicing concept
print(s[:])
print(s[1:-1])
print(s[:-5])
